{{>layouts/header}}

<style>
    .main-section {
        flex: 1;
        width: 100%;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        align-items: center; /* 수평 중앙 정렬 */
        padding-top: 20px; /* 상단 여백 (선택적) */
    }
    /* form-container 스타일 */
    .form-container {
        display: grid;
        grid-template-columns: 2fr 7fr 1.5fr; /* 레이블, 입력 필드, 버튼 비율 설정 */
        grid-template-rows: auto; /* 행의 높이를 자동으로 설정 */
        gap: 1rem; /* 요소 사이의 간격 */
        width: 70%; /* 너비를 70%로 설정 */
        max-width: 90%; /* 최대 너비 설정 (선택적) */
        height: 100%; /* 전체 높이 설정 */
        padding: 2rem;
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        box-sizing: border-box; /* 패딩과 보더를 포함한 전체 크기 조정 */
    }

    /* 폼 내부 요소의 스타일 */
    .form-container label,
    .form-container input,
    .form-container button {
        margin: 0; /* 기본 마진 제거 */
        padding: 0.5rem; /* 패딩 조정 */
    }

    /* 레이블 스타일 */
    .form-container label {
        display: flex;
        align-items: center; /* 수직 중앙 정렬 */
    }

    /* 입력 필드 및 버튼의 중앙 정렬을 위한 공통 스타일 */
    .form-container input,
    .form-container button {
        max-width: 90%; /* 너비를 100%로 설정 */
    }

    /* 각 그리드 셀을 중앙에 배치하기 위한 스타일 */
    .form-container > * {
        display: flex;
        align-items: center; /* 수직 중앙 정렬 */
        justify-content: center; /* 수평 중앙 정렬 */
    }

    /* 입력 필드 스타일 */
    .form-container .inputText {
        margin-top: 2rem;
        height: 2.5rem; /* 높이 조정 */
        font-size: 0.875rem; /* 폰트 크기 조정 */
    }

    /* 버튼 스타일 */
    .form-container button {
        margin-top: 2rem;
        height: 2.5rem; /* 높이 조정 */
        font-size: 0.875rem; /* 버튼 폰트 크기 조정 */
    }

    /* phone-container 스타일 */
    .phone-container {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr; /* 3열 구조: 010, input, input */
        gap: 0.5rem; /* 입력 필드 사이의 간격 */
        grid-column: 2 / -1; /* 두 번째 열부터 시작하여 전체 너비를 차지하도록 설정 */
        align-items: center; /* 수직 중앙 정렬 */
    }

    /* phone-container .phone-prefix 스타일 */
    .phone-container .phone-prefix {
        text-align: center; /* 가운데 정렬 */
    }

    /* submit-button-container 스타일 */
    .submit-button-container {
        grid-column: 3; /* 전체 너비를 차지하도록 설정 */
        text-align: center; /* 버튼을 중앙에 위치하도록 설정 */
    }

    /* input 클래스 스타일 */
    .input {
        grid-column: span 2; /* 두 열을 차지하도록 설정 */

    }
</style>

<script src="/js/usercheck.js"></script>


<h1> 회원가입</h1>
<form class="form-container" action="/join" method="post" id="joinForm">
    <label for="userid">Id:</label>
    <input type="text" id="userid" name="id" class="inputText" maxlength="20">
    <button type="button" class="btn" onclick="check_userId()">중복 확인</button>

    <label for="username">Username:</label>
    <input type="text" class="input inputText" id="username" name="username" maxlength="20">

    <label for="user_password">Password:</label>
    <input type="password" class="input inputText" id="user_password" name="password" maxlength="20">

    <label for="alias">Alias:</label>
    <input id="alias" name="alias" class="inputText" maxlength="20">
    <button type="button" class="btn" onclick="check_userAlias()">중복 확인</button>

    <label for="email">Email:</label>
    <input type="email" class="input inputText" id="email" name="email" maxlength="20">

    <label for="phone">Phone:</label>
    <div class="phone-container">
        <span class="phone-prefix">010</span>
        <input type="text" id="phone-part1" name="phone1" placeholder="1234" maxlength="4" pattern="\d{4}" required>
        <input type="text" id="phone-part2" name="phone2" placeholder="5678" maxlength="4" pattern="\d{4}" required>
    </div>

    <div class="submit-button-container">
        <button type="button" onclick="check_join()">회원가입</button>
    </div>
    <input id="checkId" hidden="hidden">
    <input id="checkAlias" hidden="hidden">

</form>


{{>layouts/footer}}